[project]
name = "markdownfs-docs"
version = "0.0.0"
description = "Documentation site for MarkdownFS."
requires-python = ">=3.10"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
  "pymdown-extensions>=10",
]
